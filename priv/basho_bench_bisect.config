%{mode, max}.
{mode, {rate, 1}}.

{duration, 15}.

{concurrent, 1}.

{driver, basho_bench_driver_bisect}.

{code_paths, ["../bisect/ebin"]}.

{operations, [{mget,1}]}.

{key_generator, {uniform_int, 250000000}}.

{value_generator, {fixed_bin, 1}}.
